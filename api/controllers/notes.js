import { db } from '../db.js'

export const getNotes = (req, res) => {
  return res.status(200).json({ok: 'AQUI'}) 
}

export const addNote = () => {
  
}

export const updateNote = () => {
  
}

export const deleteNote = () => {
  
} 
